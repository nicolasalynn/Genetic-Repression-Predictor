function [outputArg1,outputArg2] = conservation(seq,bs_indices)

load('genes_training.mat');
cons = genes_training.conservation;
L = length(bs_indices);
avg = 0;

for j=1:1:L
for i=0:1:6
    avg = avg + seq(j+i);
end
end

outputArg1 = inputArg1;
outputArg2 = inputArg2;
end

